{
  "name": "wealthhabit",
  "version": "1.0.0",
  "description": "WealthHabit — a personal finance app for habit-driven wealth building. Expense & income tracking, habit streaks, savings goals, net worth analytics, and an admin panel.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node -e \"(async () => { const { resetAndSeed } = require('./src/db'); await resetAndSeed(); console.log('Database reseeded.'); process.exit(0); })().catch((e) => { console.error(e); process.exit(1); })\""
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "mongodb": "^7.6.0"
  }
}
